import { FC } from 'react';
import { Layout } from 'antd';

const { Header } = Layout;

const HeaderComponent: FC = () => {
  return <Header className="layout-page-header">header</Header>;
};

export default HeaderComponent;
